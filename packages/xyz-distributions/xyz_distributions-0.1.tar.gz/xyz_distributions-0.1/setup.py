from setuptools import setup

setup(name='xyz_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['xyz_distributions'],
      author = 'Codeninja',
      author_email = 'student@udacity.com',
      zip_safe=False)
