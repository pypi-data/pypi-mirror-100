from setuptools import setup

setup(name='binomial_gaussian_distributions_v',
      version='2.0',
      description='Gaussian distributions',
      packages=['binomial_gaussian_distributions_v'],
      zip_safe=False)
