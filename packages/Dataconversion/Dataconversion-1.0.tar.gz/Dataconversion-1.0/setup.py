
import setuptools
setuptools.setup(
    name="Dataconversion",
    version="1.0",
    long_description="This is my first package which convert image to text",
    packages=setuptools.find_packages(exclude=["Test"])
                 )